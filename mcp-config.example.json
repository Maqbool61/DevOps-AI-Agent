{
  "mcpServers": {
    "devops-ai-agent": {
      "command": "devops-agent",
      "args": ["mcp"],
      "env": {
        "ORG_ID": "your-org",
        "STORAGE_PROVIDER": "memory",
        "AUTO_APPLY": "false"
      }
    }
  }
}
